mutation SetMealLogReceipt($mealLogId: UUID!, $receiptImageUrl: String, $ocrRaw: JSON) {
  updatemeal_logsCollection(
    set: { receipt_image_url: $receiptImageUrl, ocr_raw: $ocrRaw }
    filter: { id: { eq: $mealLogId } }
  ) {
    affectedCount
  }
}

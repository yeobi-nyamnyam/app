mutation UpdateNickname($id: UUID!, $nickname: String!) {
  updateprofilesCollection(set: { nickname: $nickname }, filter: { id: { eq: $id } }) {
    affectedCount
    records {
      id
      nickname
    }
  }
}

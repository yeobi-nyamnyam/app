diff --git a/build/reactNativeConfig/config.js b/build/reactNativeConfig/config.js
index 65ef8980f00d2a6e071021e61c5de85cb2c00358..91b854f15db2967b3d501ddc32cd2f95b88cf33e 100644
--- a/build/reactNativeConfig/config.js
+++ b/build/reactNativeConfig/config.js
@@ -15,6 +15,9 @@ const mockedNativeModules = path_1.default.join(__dirname, '..', '..', 'node_mod
  * Load the `react-native.config.js` or `react-native.config.ts` from the package.
  */
 async function loadConfigAsync(packageRoot) {
+    // Resolve symlinks (pnpm's virtual store) so require-from-string computes
+    // node_modules search paths from the real location, not the symlink.
+    packageRoot = await promises_1.default.realpath(packageRoot).catch(() => packageRoot);
     const configJsPath = path_1.default.join(packageRoot, 'react-native.config.js');
     if (await (0, fileUtils_1.fileExistsAsync)(configJsPath)) {
         return requireConfig(configJsPath, await promises_1.default.readFile(configJsPath, 'utf8'));

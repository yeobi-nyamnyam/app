# 기능별 담당자 배정표

킥오프 세션 체크리스트의 "기능별 담당자 배정" 항목용 뼈대입니다.
이름/기능만 채우면 폴더명은 `CLAUDE.md`의 매핑표와 자동으로 맞습니다.

> 기능명세서 v4(0812) 기준으로 갱신 — F3-5(착한가격업소 배치 캐싱), F6-10(매장명
검색·자동완성) 반영. 각 기능의 서버 협업 필요도는 `docs/api-server-boundaries.md`,
재계산 로직 상세는 `docs/business-logic-notes.md` 참고.
>

| 기능 대분류 | 기능 ID 범위 | 폴더명 | 담당자 | 비고 |
| --- | --- | --- | --- | --- |
| 회원 인증 | F0 ~ F0-5 | `auth` | 초연 |  |
| 여행 생성 | F1 ~ F1-6 | `trip-create` | 초연 | `region_cache`(정적 시드) 조회만, 라이브 API 없음 |
| 예산 산정 | F2 ~ F2-5 | `budget` | 초연 | 캐스케이드·재분배 로직 핵심 (`business-logic-notes.md` §1~2) |
| 채팅 | C0 ~ C3 | `chat` | 초연 | AI/서버 협업 필요, SSE 스트리밍 클라이언트 처리 포함. C2(자연어 파싱)가 예산 재계산과 직결되므로 `budget` 담당자와 동일인 배정 |
| 음식점 추천 | F3 ~ F3-5 | `recommend` | 희정 | **업무량 큼** — F3-5(착한가격업소 전량 수집+주소파싱+Geocoding+TourAPI 통합 배치) 포함, 서버 프록시(`recommend` API, `restaurants` 배치 스크립트) 협업 필요. 배치 스크립트는 별도 시간 배분 권장 (아래 참고) |
| 예산 수정 | F4 ~ F4-2 | `budget-edit` | 희정 | 재분배 로직(§2) 재사용, `budget` 담당자(초연)과 협업 필요 (크로스 인원 조율 포인트) |
| 게이미피케이션-배지 | G0 ~ G17 | `badges` | 희정 | 선택 기능, 우선순위 조정 가능. 판정 로직 상세는 `business-logic-notes.md` §6 |
| 기록 | F6 ~ F6-10 | `record` | 수진 | **볼륨 가장 큼** — OCR 연동, F6-10(네이버 검색 서버 프록시) 신규 추가, F6-4~F6-6 재계산 로직(`business-logic-notes.md` §1, §3, §4) 포함. `mypage` 담당자와 동일인이라 M2 좌표 스키마 조율 불필요 |
| 일기 | D0 ~ D3 | `diary` | 수진 | 기록 탭 소속 (마이페이지 아님) — `record` 담당자와 동일인 배정 |
| 여행 완료 | F7 ~ F7-1 | `trip-complete` | 수진 | 배지/포인트 트리거 지점, 지연 평가 로직은 서버(§`server-api-spec.md`) |
| 캐릭터 성장 | L0 ~ L4 | `character` | 수진 | 선택 기능, 우선순위 조정 가능. 포인트 매핑은 `business-logic-notes.md` §7 |
| 마이페이지 | M0 ~ M2 | `mypage` | 수진 | 선택 기능, 우선순위 조정 가능. M2는 `record`(수진, 동일인)가 채우는 `meal_logs.store_latitude/longitude`에 의존 (`business-logic-notes.md` §10) |
| 서버(Express 초기 세팅) | - | `apps/server` | 공통 | 미들웨어·에러 핸들링·라우터 스캐폴딩 등 1회성 기반 작업만 세션 초반에 팀 전체가 같이 처리. 이후 도메인별 라우터는 아래 매핑대로 각자 개발 |

## 서버(apps/server) 담당 매핑

`apps/server`는 별도 담당자 없이 **각 기능 담당자가 자기 도메인의 서버 라우터/프록시까지 직접 개발**합니다
(`docs/api-server-boundaries.md` 기준). Express 초기 스캐폴딩(공통 미들웨어, 인증 미들웨어, 에러 핸들링 등)만
세션 초반에 한 번 같이 세팅하고, 이후는 아래처럼 나뉩니다.

| 서버 도메인 | 담당자 | 비고 |
| --- | --- | --- |
| AI 채팅 LLM 프록시(C1, C2) + SSE 스트리밍 | 초연 | `chat_messages` 기록, 파싱 결과 검증 후 반환 |
| TourAPI·착한가격업소·네이버 Geocoding + `restaurants` 24시간 배치(F3-5) | 희정 | `restaurants` 배치는 `recommend` 담당자 혼자 시간 내 끝내기 빠듯해서, 배치 스크립트 자체는 세션 초반에 팀 전체/서버 세팅과 묶어 처리하는 걸 권장 (아래 참고) |
| 클로바 리시트 OCR + 네이버 지도 검색(F6-10, 매장명 자동완성) | 수진 | OCR은 Storage 업로드 → 서버 → OCR 호출. F6-10 결과 좌표는 `meal_logs.store_latitude/longitude`에 채움 |
| 여행 완료 시 배지/포인트 일괄 계산(G0~G17, L0~L4) | 수진 | `trip-complete`(F7) 트리거에 묶여 있어 서버 로직도 수진이 처리. `badges`(희정) 화면은 이 계산 결과만 조회 — 배지 판정 결과 스키마는 세션 초반에 희정·수진이 같이 합의 |

## 배정 시 참고

- **핵심(우선순위 최고)** 기능: F3-3(추천 기준 산정, 희정), C2(자연어 파싱, 초연), F6-4(캐스케이드 확정, 수진) — 예산 재계산과 직결. F4(희정)·F6-4(수진)는 결국 초연의 예산 캐스케이드 로직(§1~2)에 의존하니 초연이 세션 초반에 로직 문서화를 먼저 끝내는 게 우선
- **필수** 기능부터 먼저 배정하고, **선택** 기능(G, L, M, D 일부)은 시간 남으면 진행
- `recommend`(희정)과 `chat`(초연)을 서로 다른 사람에게 분산해 서버 프록시 설계 부담을 나눔 — 세 사람이 동시에 작업 가능하도록 초연(인증·여행생성·예산·채팅) / 희정(추천·예산수정·배지) / 수진(기록·일기·여행완료·캐릭터·마이페이지)으로 재배정
- **F3-5 배치 스크립트**(착한가격업소 전량 수집+Geocoding+upsert)는 `recommend` 담당자(희정) 혼자 시간 내 끝내기엔 분량이 있어서, 세션 초반에 팀 전체가 같이 붙어서 처리하고 이후 화면/서버 라우터 작업은 희정이 이어가는 방식을 제안 (`business-logic-notes.md` §8, 결정이 필요한 것 항목 참고)
- **F6-10과 M2는 스키마를 공유**하므로(`meal_logs.store_latitude/longitude`), `record`와 `mypage`를 동일인(수진)으로 배정해 조율 이슈를 없앰
- **F4(예산수정)는 희정, F2(예산산정)는 초연**으로 담당자가 다르므로, 초연이 캐스케이드 로직을 문서화/공유하는 시점과 희정의 F4 착수 시점을 세션 초반에 맞춰볼 것